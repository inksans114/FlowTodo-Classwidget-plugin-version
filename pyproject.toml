[project]
name = "flowtodo-classwidget-plugin"
version = "1.0.1"
description = "A Class Widgets plugin: FlowTodo FluentUI."
authors = [
    {name = "inksans114"},
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["class-widgets-sdk>=0.4.2"]

[tool.setuptools]
packages = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
